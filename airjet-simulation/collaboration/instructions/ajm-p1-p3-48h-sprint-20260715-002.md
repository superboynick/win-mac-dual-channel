# AirJet P1-P3 48 小时基线冲刺：Windows 执行任务 002

## 1. 身份、前次失败与本次授权

- `task_id=ajm-p1-p3-48h-sprint-20260715-002`
- `workflow_id=ajm-p1-p3-48h-sprint-20260715-r2`
- 执行端：Windows Codex；Mac/Windows 平级，GitHub `main` 是共享事实源。
- 本任务是新的 signed root task，不是旧 task ID 的 retry 或 automatic relay。
- 旧任务 `ajm-p1-p3-48h-sprint-20260715-001` 在模型启动前因 `codex.cmd` 长 argv
  失败，没有执行 AirJet 工程、没有 result commit、也不得为旧 ID 创建 receipt。
- watcher transport critical fix 已通过 Mac 80 项、Windows PowerShell 5.1 72 项，
  由 Mac-signed C1 `d8c3962aa2476f22146091a2461778fa53e01173` 单独部署；四个
  installed runtime hashes 已核对，旧 pending 已归档且旧 processed claim 保留。
- 原始本地候选 `cc6c7d323669a6f5110a66fa3a58ef5828b576cb` 从未推送，
  不是 `TASK_COMMIT`；Windows 在同一父提交上先推送了合法的
  Windows→Mac 测试，所以本 002 已从该测试 result/receipt 之后的最新
  线性 tip 重建。Windows processed ledger 中不存在 002 claim；不得尝试
  checkout、merge 或执行未推送 sibling commit。

本任务在 48 小时内形成可审计的 P1 全产品候选基线、P2 S0 结构预 Gate 基线和
P3 单胞瞬态 CFD 预 Gate 基线。它不授权伪造 P1/P2/P3 Gate PASS；完整产品始终是
主模型，single cell 仅是校准子模型。

## 2. 新任务接手 Gate

1. 保存本次 tip 为 `TASK_COMMIT`；要求 Windows 正式 repo 位于 `main`、clean，
   `HEAD=origin/main=TASK_COMMIT`、ahead/behind `0/0`，历史线性且只能 fast-forward。
2. 验证 `TASK_COMMIT` 为 Mac good SSH signature，指纹必须是：
   `SHA256:jdxP5xJrt8J7PKjeCrJmrEeoAH44u9NxBICo41HwMuc`。
3. 验证 `WINDOWS_TASK.env` 与本 instruction 是该 tip 的 `100644 blob`，schema v2
   严格等于本任务身份，且本次 incoming diff 不含 watcher、`.gitattributes`、
   `.gitmodules` 或 `MAC_TASK.env` 变更。
4. 阅读 `AGENTS.md`、两个 AirJet skills、协作协议、本文件，以及旧 instruction：
   `airjet-simulation/collaboration/instructions/ajm-p1-p3-48h-sprint-20260715-001.md`。
   只允许复用 `afd17c577c12b784c8575adeeff6d4d2cf4ae189` 中的固定版本，要求：

   ```text
   git_blob=fff0d07a093bc0bf44299109f975b3e45f003fba
   sha256=f72348f2b87ac06bc3d7097ef812ab08f390149d45939a1625677e3b84eae664
   ```

   当前 tree 中该路径必须与上述 blob/SHA 完全相同；否则停止。采用其工程范围、
   Phase A-D、证据/Gate/Claude/ANSYS/Git/教学规则，但所有 task/workflow/receipt 身份
   以本文件为准；不得创建旧 001 receipt。
5. 在 Windows 上对 repo checkout 与 installed runtime 四文件分别计算实际文件字节
   SHA256（不用 Git blob OID），要求两组一一相等，并匹配 C1 已部署值：

   ```text
   AirJetWatcher.Common.ps1=C1597D7760CDA60ADA452305EE20E749812A73B5EF48F0A600E37986B6F3CCD4
   Watch-AirJetGit.ps1=295ECFEEF182C11EDF51B21B3273594FBB08DE9F1BE0CEC106804330970557C4
   Manage-AirJetWatcher.ps1=EB1E53088F8FFF59708B6EA11896BD1D047E81665851FE08DE65BEC42538DC1D
   Run-AwakenedCodex.ps1=6D0667D7B60BB04B7BC549EE380C22F042C818A1990A69A6B96041BD59B2BBDB
   ```

6. 对照当前 Git hashes 验证两个 repo-sourced AirJet skills 的 installed 副本，只在
   hash 不一致时执行项目 installer；preflight 不做与本任务无关的官方 skills 网络
   列表/下载，外部 catalog 不可用不是 blocker。随后运行 ANSYS policy test 和项目
   audit。签名、blob/schema、trust、Git、AirJet skill hash、初始 policy 或初始 audit
   任一失败时，禁止修改 repo，准确报告 blocker。
7. 确认本任务的 START 报告、最终报告、002 receipt、教学日志与
   first-draft evidence manifest 路径在 `TASK_COMMIT` 中都不存在；任一已存在
   则停止，不得覆盖或与 001 共用产物。后两个固定路径为：

   ```text
   airjet-simulation/learning/P1_P3_48H_TEACHING_LOG_002.md
   airjet-simulation/learning/FIRST_DRAFT_EVIDENCE_MANIFEST_002.csv
   ```

接手成功后立即记录 `SPRINT_STARTED_AT_UTC` 与其后 48 小时的
`SPRINT_DEADLINE_UTC`。截止后不启动新 ANSYS job，保全证据并完成准确的 partial/final
报告。`SPRINT_COMPLETE` 只表示本轮执行闭合，不等于工程 Gate 完成。

## 3. 先产生可观察的 START 证据

完成第 2 节所有只读 Gate 后，先创建：

`airjet-simulation/reports/AJM_P1_P3_48H_SPRINT_WINDOWS_START_002_2026-07-15.md`

内容至少记录：`TASK_ID`、`WORKFLOW_ID`、`TASK_COMMIT`、`INSTRUCTION_PATH`、instruction
blob/SHA256、`TASK_SIGNER_FINGERPRINT`（Mac）、预期 `START_COMMIT_SIGNER_FINGERPRINT`
（Windows：`SHA256:oI3/MIlKz1mgLV3+5n1coQxynaqQOzxqi0GHxreGEdc`）、开始/截止
UTC、installed runtime hashes、skill/policy/audit 结果、创建 START 文件前的 repo
preflight `HEAD`/clean/ahead-behind `0/0` snapshot 及 UTC、runner 已读取本 signed
instruction、Codex 版本、GUI 状态 `NOT_USER_OBSERVED`。另记录：001 在模型前失败
而 002 使用 UTF-8 stdin 修复；初始状态 `P0=PASS / P1-P6=NOT_RUN`；当前 alternate route
和 `P1 full-product pilot → P2 S0 displacement → P3 single-cell calibration` 依赖。
不得把 transport、启动或 audit 写成任何工程 Gate PASS。

fetch 并确认远端仍为 `TASK_COMMIT` 后，把该 START 报告做成 Windows-signed commit，
验证 good signature 并正常 push。该 commit 只是仓库内“真实 Codex 已进入任务”的
最早证据，不能单独证明 watcher E2E；最终还需结合 Git 外 pending/processed、runner
final report、exit 0 与 watcher terminal 状态闭合。保存其 full hash 为
`START_COMMIT`；push 后继续工作，不等待 Mac 回应。

## 4. 并行执行约束

- Windows 主 Codex 独占写入、ANSYS、集成、验证、commit/push 和最终判断。
- 最多两个只读 subagent 并行：A 审计 run #22/route evidence；B 审计 P1-P3
  contracts/tests/Gate/教学文档。subagent 不改文件、不跑 ANSYS、不提交。
- 代码、profiles、tests、文档和只读审计并行；SpaceClaim/Workbench/Mechanical/Fluent
  真实 jobs 因 Student license 与机器锁严格串行。
- Claude CLI 仅通过 `tools/claude-cli/review-staged.ps1` 做无工具只读复核，模型只用
  `deepseek-v4-pro`/`deepseek-v4-flash`；Codex 独立验证。不得发送商业 PDF、private
  bundle、raw solver data、许可或凭据。Claude 不可用不是工程 blocker。
- 不读取/修改 license pool、许可服务、注册表许可、许可环境变量或 checkout 路线；
  不添加 override，不通过删除 cell/孔/关键流路规避 Student 限制。

## 5. 必须执行的工程主线

严格执行旧 instruction 第 5-10 节，以下边界再次固定：

1. 先归档 run #22：精确分类 `RUNSCRIPT_RETURNED_ENTRY_ABSENT`；最强结论只是 direct
   `RunScript` 返回但 child entry/build 未观察到。connected route 标记
   `DEFERRED_CURRENT_HOST_ROUTE`，本冲刺不再追加 connected 探针。
2. 主路线迁移为：

   ```text
   CAD_AUTHORING_ROUTE=SPACECLAIM_SIGNED_SCRIPT_PARAMETRIC
   SOLVER_HANDOFF_ROUTE=HASH_BOUND_STEP_SEMANTIC_SIDECAR
   EXTERNAL_NATIVE_ATTACH=NOT_PROVEN
   NATIVE_PARAMETERIZATION=NOT_PROVEN
   NATIVE_NAMED_SELECTION_TRANSFER=NOT_PROVEN
   ```

   route-dependent STEP 是 hard requirement；Workbench transfer 只能表示 STEP import +
   solver-side semantic reconstruction，不能冒充 native named-selection transfer。
3. 先完成 alternate-route 005 closeout；相关 profile/script/runner/schema/判定合同任一
   hash 改变后，必须在冻结提交之后补组合 confirmation run，不能用旧 raw evidence
   证明新实现。
4. P1 只先执行完整产品 pilot `M-3x4-7.0__R50_BALANCED`，包含全部 12 cells、
   vents/plenum/gaps/chambers/orifices/impingement/manifold/spout。pilot 与正式 006/007
   分离；状态只能是：

   ```text
   P1_ACTIVITY_STATUS=P1_CANDIDATE_ONLY/PARTIAL_CAD_OUTPUT
   P1_STAGE_GATE=INCOMPLETE
   ```

   正式 4 configurations、9 variants、252-row worksheet 和 007 review 合同不降低。
5. 并行准备 P2 S0 与 P3 single-cell profiles/runners/tests/docs；P1 pilot 可被 solver
   消费后，真实 ANSYS jobs 先 P2 后 P3 串行。P1 Gate 以前固定为：

   ```text
   P2_ACTIVITY_STATUS=PRE_GATE_BASELINE_ONLY
   P3_ACTIVITY_STATUS=PRE_GATE_BASELINE_ONLY
   P2_STAGE_GATE=NOT_RUN
   P3_STAGE_GATE=NOT_RUN
   ```

6. P2 优先产出可追溯位移-时间/幅频结果；压电耦合能力不足时使用明确标注的等效驱动
   baseline，并保留 `NOT_FULL_PIEZOELECTRIC_FSI`。
7. P3 优先使用 P2 位移驱动 dynamic mesh；若只有解析波形，只能标
   `NOT_GATE / ANALYTIC_WAVEFORM_ONLY`。mesh/time-step independence 未完成时保持
   `BASELINE_ONLY / INDEPENDENCE_PENDING`。
8. 大型 case/data/mesh/raw ZIP 留在 Git 外，Git 只保存小型摘要、SHA256、大小、路径
   指针和重建命令。每个 job 必须有唯一 ID、时间、版本、profile/script/input/output
   hashes、return code、assertions、分类和进程清理记录。
9. START 时记录实测物理内存、available memory 和前十个内存消耗进程，
   但不读取其私密内容、不终止任何无关用户应用或可能含有未保存数据
   的进程。P1/P2 和 P3 准备不因内存压力停止。真实 P3 transient job
   启动前重测；available memory 小于 24 GiB 时不启动中等或更大的
   dynamic-mesh case，而是继续所有安全准备、可行的小网格 smoke 与教学留档，
   并精确标记 `RAM_PRESSURE_BLOCKS_P3_BASELINE`。不得因赶时间隐瞒 OOM 风险。
10. 从 START 就创建并随每个阶段更新上述教学日志和 evidence manifest。
    日志按“输入→模型/方程→数值检查→输出→不确定性→能写/禁写”讲解；
    manifest 每行必须绑定 claim/figure/table、source path、run ID、SHA256、单位、
    D/P/I/C/U、Gate 状态、`AVAILABLE_FOR_WRITING/NOT_RUN`、允许与禁止措辞。
    用户亲自写论文；Windows Codex 不代写 first draft，只交付方法教学、图表源
    数据、证据对照和可写边界。

## 6. 连续执行、阶段提交与恢复

- 建议阶段：START；A=run22 archive/freeze；B=route contract/005 closeout；
  C=P1 pilot；D=P2/P3 preparation；E=P2/P3 results/final report。
- 每次提交前 fetch；只接受远端是当前 Windows HEAD 的线性状态。所有 dirty paths 必须
  属于本任务；运行相称的 compile/tests/policy/audit/diff-check，关键 staged diff 用
  Claude reviewer（可用时）复核。做 Windows SSH-signed commit，验证签名再正常 push。
- START、各阶段、result 和 receipt commit 都必须验证为同一 Windows signer
  `SHA256:oI3/MIlKz1mgLV3+5n1coQxynaqQOzxqi0GHxreGEdc`；仅有泛化的
  `Good signature` 文字不足够。
- 不 reset/rebase/merge/force-push，不修改本 task envelope/instruction，不创建 reciprocal
  task。远端出现未知提交或分叉立即 fail closed。
- 同一 task ID 只自动唤醒一次。断电/Codex 崩溃后从最新 signed phase commit 与 Git 外
  run evidence 恢复；若 runner 已退出，需用户手动续跑或新的 Mac root task。
- 不因普通 job failure、超时或 connected route 失败停止整个冲刺；保存证据，使用已定义
  fallback 并继续安全可执行的并行工作。仅真实 authority/Git/trust/许可/硬件 blocker
  可以终止。

## 7. 最终报告与本任务 receipt

最终综合报告：

`airjet-simulation/reports/AJM_P1_P3_48H_SPRINT_WINDOWS_002_2026-07-15.md`

必须列出 `TASK_COMMIT`、`START_COMMIT` 和各阶段 commits，但不得要求该报告写入
承载它自身的 `RESULT_COMMIT`。报告还必须列出实际 job 清单、P1/P2/P3 精确状态、
直接证据、失败/限制、下一步、Git 外大型证据 paths/hashes、Claude 状态、签名/audit/
commit 结果，以及创建最终报告文件前的 base `HEAD`/clean/ahead-behind `0/0`
snapshot 与 UTC。另列 `ENGINEERING_GATES_NOT_COMPLETE`；GUI 未由用户肉眼确认时
保持 `NOT_USER_OBSERVED`。报告为每个阶段固定提供“目标 / 实际完成 / 直接证据 /
可宣称 / 不可宣称 / 下一步”六栏表，使用户能从工程决策与现实失败中学习。
最终报告还必须链接教学日志与 evidence manifest，明确列出用户当时
已能写的论文部分、仍只能保留 placeholder 的图表以及其直接缺失证据。

结果提交并 push 后保存 `RESULT_COMMIT`，再创建且只创建：

`airjet-simulation/collaboration/receipts/ajm-p1-p3-48h-sprint-20260715-002.env`

使用 collaboration schema，固定：

```text
schema_version=1
type=result_receipt
workflow_id=ajm-p1-p3-48h-sprint-20260715-r2
task_id=ajm-p1-p3-48h-sprint-20260715-002
task_commit=<saved TASK_COMMIT>
executor=windows
status=complete
result_commit=<saved RESULT_COMMIT>
summary_path=airjet-simulation/reports/AJM_P1_P3_48H_SPRINT_WINDOWS_002_2026-07-15.md
next_task_id=NONE
```

receipt 的 task/result commits 必须是保存值，receipt commit 的直接 parent 应为
`RESULT_COMMIT`；做独立 Windows-signed receipt commit 并正常 push。`status=complete`
只表示冲刺与报告闭合，不代表工程 Gate PASS。
推送 result 后的 `RESULT_COMMIT`只写入 receipt；推送 receipt 后的 clean、
ahead/behind `0/0` 和 `GIT_READY=<full receipt commit>` 由 runner 的 final response 报告，
不要尝试把未知的自身 commit hash 回写到承载它的文件。

真实 blocker 时可提交准确 blocker 报告，但不得创建 complete receipt。否则持续执行到
receipt 推送完成，并报告 `GIT_READY=<full receipt commit>`；不要等待用户逐步指挥。
