# AirJet P1-P3 48 小时基线冲刺：Windows 执行任务 001

## 1. 任务身份与最终目标

- `task_id=ajm-p1-p3-48h-sprint-20260715-001`
- `workflow_id=ajm-p1-p3-48h-sprint-20260715`
- 执行端：Windows Codex；Mac Codex 与 Windows Codex 平级，GitHub `main` 是唯一共享事实源。
- 时间目标：在 48 小时冲刺中形成可审计的 P1 全产品候选基线、P2 S0 结构基线和 P3 单胞瞬态 CFD 基线。
- 论文与 Gate 边界：本任务追求 `BASELINE`，不把候选模型、smoke、单次运行或未完成独立性验证的结果写成 P1/P2/P3 正式 Gate `PASS`。
- 产品目标始终是 AirJet Mini-class 完整产品；单胞仅作为结构/CFD 校准子模型，不能替代完整产品目标。

Windows Codex 接手成功后立即记录 `SPRINT_STARTED_AT_UTC`，并把其后 48 小时写为
`SPRINT_DEADLINE_UTC`。到截止时间后不再启动新的 ANSYS job；保全正在运行或已生成
的证据，完成准确的 partial/final 报告和安全 Git 收尾。`SPRINT_COMPLETE` 只表示本轮
执行与报告闭合，不表示任何工程 Gate 已完成。

持续执行本任务各阶段，直到结果提交与 receipt 推送完成，或出现经过安全检查仍无法绕过的真实 blocker。不要因普通失败、长时间求解或某条自动化路线受阻就停下来询问用户；按本任务的 fallback 继续。

## 2. 接手前必须通过的信任检查

1. 阅读仓库根目录 `AGENTS.md`、本文件、`airjet-simulation/collaboration/README.md`、两个 AirJet skills 和当前项目状态/现实失败日志。
2. 要求正式 Windows 仓库位于 `main`、工作树 clean、相对 `origin/main` 为 `0 ahead / 0 behind`，且只能 fast-forward。
3. 保存本任务 tip 为 `TASK_COMMIT`，验证它是 Mac 签名的 good SSH signature，签名指纹必须是：
   `SHA256:jdxP5xJrt8J7PKjeCrJmrEeoAH44u9NxBICo41HwMuc`。
4. 验证 `WINDOWS_TASK.env` 与本 instruction 都是 `TASK_COMMIT` 中的 `100644 blob`，信封严格满足 schema v2，且这个 task commit 就是目标 tip。
5. 运行官方 skill 安装/同步、ANSYS policy test 和项目 audit。签名、task blob/schema、branch/divergence、trust、初始 policy test 或初始 project audit 任一失败时，禁止修改仓库并立即报告 blocker；只有全部接手 Gate 通过以后，task-owned 实现或测试失败才允许在本任务范围内修复。
6. 不修改 `WINDOWS_TASK.env`、本 instruction、Mac/Windows trust root、watcher 源码或 watcher 部署。不得 reset、rebase、merge、force-push 或覆盖对端提交；发现远端分叉立即 fail closed。

## 3. 并行方式与资源约束

- Windows 主 Codex 独占写入、ANSYS 操作、验证、提交和推送决定。
- 最多启用两个只读 subagent：
  - A：审计 run #22 归档、分类边界和证据完整性；
  - B：审计 P1-P3 route contract、profiles、tests、Gate 语言和教学文档。
- subagent 不写文件、不运行 ANSYS、不提交；主 Codex 合并并验证建议。
- ANSYS/SpaceClaim/Workbench/Mechanical/Fluent 求解任务严格串行，避免 Student license 和机器锁冲突；代码、合同、教学文档与只读审计可以并行。
- 每次真实 ANSYS job 前后检查并记录项目所属进程；只终止能证明属于本项目且已经失控的进程，不碰用户的其他 ANSYS/Claude/Codex 进程。
- 上一轮通过 SSH 发起的 Claude review 被中止。启动新 Claude review 前，只读检查是否还存在明确属于该项目、命令行可识别的遗留 Claude 进程；不要仅凭进程名杀进程。

## 4. Claude CLI 的限定用法

Claude CLI 只作为第二模型只读 reviewer，Codex 负责全部判断和修改。必须使用仓库的 Git-external wrapper；不得提交本机 token、gateway、settings、session、cache 或原始私密输出。

关键 diff 暂存后使用：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\claude-cli\review-staged.ps1 -Model deepseek-v4-pro -Effort high airjet-simulation/PROJECT_STATUS.md
```

- `deepseek-v4-pro`：route contract、ANSYS runner、Gate 判定和最终关键 diff。
- `deepseek-v4-flash`：可用于宽范围、低风险一致性扫描。
- 示例中的路径应替换或追加为本次暂存的 repo-relative narrow paths；不要把尖括号占位符直接交给 PowerShell。
- review 必须禁用工具；Codex 必须逐条用源码、测试或原始证据复核，不能直接采信模型结论。
- 不得把商业 PDF、private research bundle、raw solver data、许可信息或任何凭据发送给 Claude gateway。
- 如果 CLI 未登录、超时或 wrapper 失败，准确记录为 review unavailable，继续 Codex 本地审计；这不是工程 Gate blocker。

## 5. Phase A：归档 run #22 并冻结 connected 路线

把刚完成的 connected SpaceClaim `Interactive=True + RunScript-only` 诊断 run #22 归档为小型 Git 证据；大型 raw ZIP、ANSYS 临时目录和 solver cases 留在 Git 外，仓库仅保存 SHA256、大小、绝对 Windows 路径和可重定位说明。

必须保持以下已观察事实，不升级结论：

- Suite：`AJM005_T1_CONNECTED_SC_SUITE_20260715T021529059815Z_aa1180f6`
- Case：`a5c-eedabacc1fc6`
- producer：`PASS_PARTIAL_CAD_CAPABILITY`，8 个断言为真；connected Workbench job：FAIL。
- 精确分类：`RUNSCRIPT_RETURNED_ENTRY_ABSENT`。
- `RunScript` 调用返回，但 post-RunScript、Exit、failure-pre、failure-post 均未观察到 entry sentinel 或 build capture，且没有 probe error。
- connected build contract 为 `CALLED`；share、`GetGeometryFileAndSaveData`、Refresh、Mechanical、mesh、project save 全部 `NOT_REACHED`。
- 根错误：`FAIL_RUNSCRIPT_RETURNED_ENTRY_AND_BUILD_ABSENT`。
- 允许的最强结论：调用已到达并返回，但 child entry 没有被观察到。
- 禁止推断：不能写成几何 build 已执行并失败、`.py` 不受支持、connected transfer 已失败、或 P1 已通过。

更新 run index、`PROJECT_STATUS.md`、现实失败日志、ANSYS/005 教学文档、CAD transfer workbook、论文方法-证据图。然后把当前 connected external-geometry 路线标成 `DEFERRED_CURRENT_HOST_ROUTE`：48 小时冲刺内不再运行 marker-only 或新的 connected 探针。

## 6. Phase B：迁移可执行 route contract

把当前主路线明确迁移为：

```text
CAD_AUTHORING_ROUTE=SPACECLAIM_SIGNED_SCRIPT_PARAMETRIC
SOLVER_HANDOFF_ROUTE=HASH_BOUND_STEP_SEMANTIC_SIDECAR
EXTERNAL_NATIVE_ATTACH=NOT_PROVEN
NATIVE_PARAMETERIZATION=NOT_PROVEN
NATIVE_NAMED_SELECTION_TRANSFER=NOT_PROVEN
```

要求：

1. 保留旧 005 connected route 的历史证据和诊断合同，不重写历史。
2. 新增/更新 profiles、runner、policy tests、checklists、005 closeout、006 和后续 007 入口，使主路线是：签名 SpaceClaim/Discovery 脚本建模 → native save/reopen 检查 → STEP export → hash-bound semantic sidecar → Mechanical/Fluent 侧语义重建。
3. 一旦使用 STEP+sidecar，G4 中 STEP 不再是“可选 limitation”，而是 route-dependent hard requirement。
4. G4 Workbench transfer 的含义改为“STEP import + solver-side semantic reconstruction 已验证”，不能表述成 native named-selection transfer。
5. sidecar 不只保存全局 centroid/area。对每个完整产品实体或表面至少包含：稳定 feature/semantic key、owner、cell index、局部 cell 坐标、几何类型、方向/法向约束、邻接或拓扑关系、期望 cardinality、CAD/STEP/sidecar hashes、单位和 profile hash。验证器必须拒绝 duplicate/missing/unexpected keys、错误 cardinality、孤立关键表面和 hash 不一致。
6. route-dependent Gate 规则与 profiles 必须由测试锁定；所有 `NOT_PROVEN` 字段在没有直接证据时保持原值。

先完成一个 alternate-route 005 closeout。只有相关 producer/consumer profile、script、runner、sidecar schema 与判定合同的 hashes 全部未变，旧 semantic/raw evidence 才能复用；任一字节或合同发生变化，必须在合同冻结提交之后补一次新的组合 producer + semantic reconstruction confirmation run。为 006 生成 unique-key/cardinality 报告。005 closeout 不能写成 P1 stage PASS。

## 7. Phase C：P1 全产品 primary-balanced pilot

建立与正式 006/007 分离的 pilot profile/task/status，只先执行 primary-balanced 变体：

```text
M-3x4-7.0__R50_BALANCED
```

该 pilot 必须是完整产品，不是单胞阵列占位物。至少显式建模/表达：12 个 cell 的位置与局部坐标、振膜/腔体/喷口或射流开口、进气/回流通道、plenum、cell gaps、orifices、impingement/目标表面、manifold、出口 spout 和外壳/边界接口。几何细节、压缩和简化必须能从 profile 与 sidecar 追溯。

必须产出并验证：

- native archive/save-reopen 结果；
- STEP；
- hash-bound semantic sidecar；
- feature/topology/cardinality/adjacency 报告；
- Mechanical 侧 STEP import + semantic reconstruction 证据；
- 必要的 meshability smoke 与截图/原始产物指针；
- 运行时长、机器/ANSYS 版本、profile hash、脚本 hash、输入输出 SHA256 和 failure/retry 记录。

当前 48 小时 pilot 只允许状态：

```text
P1_ACTIVITY_STATUS=P1_CANDIDATE_ONLY/PARTIAL_CAD_OUTPUT
P1_STAGE_GATE=INCOMPLETE
```

斜杠表示根据直接证据二选一，不是新的 Gate 枚举。不能因 pilot 产出就将 P1 Gate 记为 PASS 或 `PENDING_PEER_REVIEW`。正式 006 的 4 configurations、9 variants、252 行 evidence worksheet 与 007 独立复核合同保持不变；只有全部正式要求完成时才允许进入 peer review。route-dependent STEP 行必须 hard-fail，不能写成 `LIMITATION_ACCEPTED`。balanced pilot 成功前不要展开完整九变体矩阵。

## 8. Phase D：并行准备并串行运行 P2/P3 基线

在 P1 pilot 构建/求解以外的时间并行准备 hash-pinned、测试覆盖的 P2/P3 profiles、runner、schema、判定器、运行日志模板和教学注释；真实 ANSYS jobs 仍串行。

P1 Gate 通过以前，本任务中的 P2/P3 都是预 Gate 研究，固定状态边界为：

```text
P2_ACTIVITY_STATUS=PRE_GATE_BASELINE_ONLY
P3_ACTIVITY_STATUS=PRE_GATE_BASELINE_ONLY
P2_STAGE_GATE=NOT_RUN
P3_STAGE_GATE=NOT_RUN
```

它们可以验证方法和数据接口，但不得升级成正式 P2/P3 stage evidence。

### P2 S0 结构基线

- 以可追溯的 S0 膜片/致动结构参数建立 PyMechanical 或受审计 Workbench 模板。
- 至少记录材料、层厚、边界、压电/等效驱动假设、mesh、求解器设置、模态频率和谐响应/位移输出。
- 做一项合理的最小 mesh sensitivity 或清楚标为未完成；单次收敛不能写成独立性通过。
- 产出供 P3 使用的位移-时间或幅频结果，并绑定 profile/script/result hashes。
- 若 Student 功能或现有材料数据阻止压电耦合，使用明确标注的等效驱动 baseline，保留 `NOT_FULL_PIEZOELECTRIC_FSI` 限制并继续。

### P3 单胞瞬态 CFD 基线

- P3 是校准单胞，不改变完整产品为总目标。
- 优先使用 P2 的位移输出驱动 dynamic mesh；若 P2 输出暂不可用，只能以解析波形完成 smoke，并标记 `NOT_GATE / ANALYTIC_WAVEFORM_ONLY`。
- 记录可压缩性/湍流/能量方程选择、fluid properties、边界、moving wall、dynamic mesh、时间步、每周期步数、周期数、收敛和质量守恒。
- 至少输出 jet velocity、pressure rise/flow rate、主要流场和周期稳定性指标；所有值都必须带单位和 provenance。
- 若 48 小时内不能完成 mesh/time-step independence，保留 `BASELINE_ONLY / INDEPENDENCE_PENDING`，不要降低条件来制造 PASS。

执行顺序：P1 pilot 达到可被 solver 消费的候选状态后，先串行执行 P2 S0 baseline，再执行 P3 single-cell transient baseline。一个求解失败时，先保存原始日志和分类，再使用任务中已定义的简化 fallback；不要重复无边界试错。

整个长期任务不得读取或修改 ANSYS license pool、许可服务、许可注册表、许可环境变量或 checkout 路线，不得添加 license override。不得通过删除 cell、孔、关键流路或其他完整产品特征来绕开 Student 限制；若能力/规模受限，降核数、降低 pilot 网格成本、分离校准子模型并准确保留限制。

## 9. 记录、教学文档和论文可用性

边做边更新教学材料，使用户之后能理解，而不是只留下脚本。至少解释：

- 每个参数来自哪一份公开来源、专利、论文、官方规格或工程假设；
- 为什么选择当前 route、模型、算法、网格、时间步和判据；
- 哪些量是直接证据、反推值、校准值或未知量；
- 每次现实失败的现象、可复现实验、排除项、结论边界、fallback 和对论文方法的影响；
- 完整产品、P2 结构子模型和 P3 单胞校准模型之间如何传递参数与不确定性；
- 论文现在能写什么、不能写什么、后续需要哪些 sensitivity/validation 才能升级 Gate。

保留详细 rationale 与 evidence trail，但不要声称保存模型不可见的私有 chain-of-thought。写出可审计的工程决策理由、备选方案、证据和判据即可。

大型 solver case、raw ZIP 和可再生成中间文件留在 Windows Downloads/项目专用 evidence 目录，Git 中保存小型摘要、hash manifest、指针和重建命令。不得提交许可文件、密钥、token、机器私密配置或来源不明的二进制安装包。

## 10. 提交、复核与阶段恢复

允许 Windows 主 Codex按可恢复的自然阶段做多个 Windows-signed commits，并正常 push；每次必须：

1. fetch，确认线性历史、没有未知改动、所有 dirty paths 都属于当前 task、无对端新提交或分叉；
2. 运行与改动相称的 Python compile、policy tests、runner unit tests、project audit 和 Git diff-check；
3. 对关键暂存 diff 运行限定 Claude reviewer，并由 Codex复核；若不可用则记录；
4. 做 Windows SSH signature commit，验证 good signature 与 Windows signer fingerprint；
5. 正常 push，不 force；保存 commit hash 到 run/report。

建议阶段提交：A=run22 archive/freeze，B=route contract/005 closeout，C=P1 pilot，D=P2/P3 preparation，E=P2/P3 results/final report。实际可合并相邻阶段，但不得为凑提交牺牲验证。

每个真实 ANSYS 运行必须有唯一 run/job ID、输入输出 hash、开始/结束时间、return code、断言结果、分类、原始证据指针和进程清理记录。断电或 Codex 重启后从 Git 最新已验证阶段和 Git 外 run evidence 恢复，禁止覆盖旧 run。watcher 的 processed ledger 对同一 `task_id` 只自动唤醒一次；若 runner/Codex 已退出，不能等待同一任务自动重唤醒，必须由用户手动续跑或由 Mac 签发新的 root task。

## 11. 最终交付与 receipt

最终综合报告固定为：

`airjet-simulation/reports/AJM_P1_P3_48H_SPRINT_WINDOWS_2026-07-15.md`

报告至少列出：`TASK_COMMIT`、所有阶段 commit、实际运行清单、P1/P2/P3 精确状态、直接证据、失败/限制、下一步、Git 外大型证据路径与 hashes、Claude review 状态、签名/审计/clean/0-ahead-0-behind 状态。用户未亲眼确认 Windows Codex 窗口时，GUI 只能写 `NOT_USER_OBSERVED`。

全部结果提交并推送后保存最后结果 commit 为 `RESULT_COMMIT`，再创建且只创建一份不可变 receipt：

`airjet-simulation/collaboration/receipts/ajm-p1-p3-48h-sprint-20260715-001.env`

使用 `collaboration/README.md` 的 schema，要求：

```text
executor=windows
status=complete
summary_path=airjet-simulation/reports/AJM_P1_P3_48H_SPRINT_WINDOWS_2026-07-15.md
next_task_id=NONE
```

receipt 的 `task_commit` 与 `result_commit` 必须分别是保存的 `TASK_COMMIT` 与 `RESULT_COMMIT`；receipt commit 的直接 parent 应是 `RESULT_COMMIT`。再次 fetch/ahead-behind 验证后，做独立 Windows-signed receipt commit 并正常 push。

receipt 的 `status=complete` 只表示冲刺执行、证据保全和报告闭合；最终报告必须另列 `ENGINEERING_GATES_NOT_COMPLETE` 及每个 Gate 的真实状态。若真实 blocker 阻止完成，准确记录已完成阶段、失败次数、原始证据、最后安全 Git 状态和恢复命令；不得伪造 `status=complete`、Gate PASS 或 receipt。blocker 场景允许提交准确的 blocker 报告，但不得创建 complete receipt。向主协调会话报告最终 `GIT_READY=<full receipt commit>` 或精确 blocker。
