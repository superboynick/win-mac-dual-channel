# Plan A continuous ANSYS readiness and MCP cancellation diagnosis

TASK_ID=`ajm-plan-a-continuous-readiness-20260720-011`

Windows A must remain continuously productive. Track B is read-only and must not duplicate
this work. Task 010 proved two identical official inventory calls were cancelled before any
ANSYS submission. Do not make a third identical call in this task and do not wait idle.

1. Produce a Git-external task card within 15 minutes with actual/expected time, ETA,
   checkpoints, blockers and at least three safe A-only backlog items.
2. Diagnose the cancellation path read-only: record the executing Codex/session identity,
   official MCP configuration source, server parent/child process mapping, start/exit times,
   and whether cancellation occurs before transport, during approval dispatch, or after the
   server receives a request. Do not inspect or expose credentials, licenses or private keys.
3. Audit the approved profile and runner path without invoking ANSYS. Verify the reviewed
   producer/profile SHA `8f23d7d7dd66efcf06909341a45a76caccd6732cbf11fa1f54157699d55228b0`,
   forbidden-marker absence, external artifact routing, dependency-manifest routing, timeout,
   terminal polling, native reopen, STEP reopen and fail-closed assertion coverage.
4. Run all relevant source-only unit, static policy, producer guard and project audit checks
   available without official MCP runtime. Preserve literal outputs and hashes externally.
5. Prepare, but do not fabricate, the next runtime acceptance checklist for 4 inlets and
   1 outlet; V01/V02 preserved; Y `-14.500 -> -17.750 mm`; backward extension `3.250 mm`;
   one closed/manifold body; expected bbox and volume `469.4396438426395 mm3`; native/STEP
   reopen and connectivity.
6. If a distinct, evidence-backed correction to the MCP cancellation path is available and
   requires no trust weakening, apply only the smallest reversible configuration/runtime
   correction outside the repository, then make one discriminating official inventory call.
   Otherwise report the exact required peer/user action and continue the safe A backlog.

Write small outputs under `C:\Users\admin\Downloads\AirJetGitWatcherReports`; keep generated
artifacts out of Git. Do not fetch, branch, sign, push, copy keys, modify the CAD producer or
profile, start Fluent, or claim any P1--P6 Gate. Report progress at 15/45/90 minutes and never
wait for Track B.
