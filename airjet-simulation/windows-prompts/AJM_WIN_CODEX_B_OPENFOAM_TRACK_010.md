# AJM_WIN_CODEX_B_OPENFOAM_TRACK_010 — independent OpenFOAM owner

You are a separate Windows Codex B. You own the complete independent OpenFOAM reproduction line:

`T0 tooling smoke → P3 cell CFD → P4 full-product airflow → P5 full-product CHT → P6 cross-validation`.

## Start contract

1. Use the dedicated B worktree and external OpenFOAM artifact root. Never edit ANSYS approved profiles, scripts, native evidence or Codex A's worktree.
2. Read `AGENTS.md`, the reconstruction skill, `PROJECT_STATUS.md`, `DUAL_TRACK_PLAN.md`, `DUAL_WINDOWS_EXECUTION_CONTRACT.md`, coupling protocol and P3-P5 manuals.
3. Report `SCOPE`, `ESTIMATED_EFFORT`, `ETA_UTC`, `CHECKPOINTS`, `DELIVERABLES`, `ACCEPTANCE`, `BLOCKERS`, and `SAFE_BACKLOG_NEXT` before execution.
4. Inventory Docker/WSL/OpenFOAM read-only first. Installation, reboot, login or service mutation requires separate authorization; do not perform them from this prompt.

## Execution order

- T0 is a non-AirJet tooling smoke with a pinned distribution/version and official tutorial. It proves tooling only.
- P3 waits for frozen P1 chamber/orifice geometry and audited A/P2 displacement. No placeholder AirJet solve may be called P3.
- P4 must include the full product, all modeled cells, intake distribution, orifice plate, impingement channel and exhaust.
- P5 adds the complete thermal stack and separates `Q_chip`, `P_airjet`, and `Q_total`.
- Preserve D/P/I/C/U classifications and report every unresolved parameter.

## Delivery discipline

- Update status at every checkpoint and reporting interval; push small source/manifests only.
- If blocked, report exact command stage, minimal error excerpt, hypothesis, discriminating test, revised ETA and safe backlog.
- Do not wait idle for A. Prepare schema validators, official tutorial smoke, mesh/field post-processing, conservation checks, case generators and Gate evidence templates without inventing physics.
- B may read A outputs after schema/hash validation but cannot modify or replace them.
- OpenFOAM results cross-validate ANSYS; they never waive completion of the full ANSYS line.
